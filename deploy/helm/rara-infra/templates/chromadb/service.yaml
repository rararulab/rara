{{- if .Values.chromadb.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rara-infra.fullname" . }}-chromadb
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rara-infra.componentLabels" (dict "root" . "component" "chromadb") | nindent 4 }}
spec:
  type: {{ .Values.chromadb.service.type }}
  ports:
    - name: http
      port: {{ .Values.chromadb.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "rara-infra.componentSelectorLabels" (dict "root" . "component" "chromadb") | nindent 4 }}
{{- end }}
