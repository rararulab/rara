{{- if .Values.mem0.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rara-infra.fullname" . }}-mem0
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rara-infra.componentLabels" (dict "root" . "component" "mem0") | nindent 4 }}
spec:
  type: {{ .Values.mem0.service.type }}
  ports:
    - name: http
      port: {{ .Values.mem0.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "rara-infra.componentSelectorLabels" (dict "root" . "component" "mem0") | nindent 4 }}
{{- end }}
