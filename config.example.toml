# Job Application Platform — Static Configuration
#
# Copy this file to `config.toml` and adjust values for your environment.
#
# Configuration priority (highest first):
#   1. Legacy environment variables (DATABASE_URL, MINIO_ENDPOINT, etc.)
#   2. JOB__-prefixed environment variables (JOB__DATABASE__DATABASE_URL, etc.)
#   3. config.toml (this file)
#   4. Code defaults
#
# NOTE: Runtime-changeable settings (OpenRouter API key, Telegram bot token)
# are managed via the web API at POST /api/v1/settings, NOT in this file.
# Environment variables OPENROUTER_API_KEY, OPENROUTER_MODEL, TELEGRAM_BOT_TOKEN,
# TELEGRAM_CHAT_ID are used as initial defaults when the DB has no persisted settings.

[database]
database_url = "postgres://postgres:postgres@localhost:5432/job"
max_connections = 10
min_connections = 1
connect_timeout_secs = 30
max_lifetime_secs = 1800    # 30 minutes
idle_timeout_secs = 600     # 10 minutes

[http]
bind_address = "127.0.0.1:3000"
# max_body_size: accepts human-readable sizes like "100MiB" or raw bytes
max_body_size = "100MiB"
enable_cors = true
request_timeout = 60

[grpc]
bind_address = "127.0.0.1:50051"
server_address = "127.0.0.1:50051"
max_recv_message_size = "512MiB"
max_send_message_size = "512MiB"

[minio]
endpoint = "http://localhost:9000"
bucket = "raramarkdown"
access_key = "minioadmin"
secret_key = "minioadmin"
region = "us-east-1"

[crawl4ai]
url = "http://localhost:11235"

# Saved-job GC interval in hours
gc_interval_hours = 24

# Graceful shutdown on SIGTERM/Ctrl+C
enable_graceful_shutdown = true

# Main service HTTP base URL (for telegram bot -> main service calls)
main_service_http_base = "http://127.0.0.1:3000"
